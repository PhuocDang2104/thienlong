import { AlertCircle, Inbox, LoaderCircle } from "lucide-react";
import { cn } from "@/lib/utils";
export function Alert({ children, tone = "error" }: { children: React.ReactNode; tone?: "error" | "success" | "info" }) { return <div role={tone === "error" ? "alert" : "status"} className={cn("flex items-start gap-2 rounded-lg border p-3.5 text-sm font-medium leading-6", tone === "error" ? "border-red-200 bg-red-50 text-red-800" : tone === "success" ? "border-emerald-200 bg-emerald-50 text-emerald-800" : "border-blue-100 bg-blue-50 text-primary")}><AlertCircle className="mt-1 size-4 shrink-0"/><div>{children}</div></div>; }
export function Spinner({ label = "Đang tải dữ liệu…" }: { label?: string }) { return <div role="status" className="flex items-center justify-center gap-2 p-10 text-sm text-muted"><LoaderCircle className="size-4 animate-spin"/>{label}</div>; }
export function EmptyState({ title, description, children }: { title: string; description?: string; children?: React.ReactNode }) { return <div className="flex flex-col items-center px-5 py-14 text-center"><Inbox className="mb-4 size-7 text-slate-400"/><p className="font-medium">{title}</p>{description && <p className="mt-2 max-w-md text-sm leading-6 text-muted">{description}</p>}{children && <div className="mt-5">{children}</div>}</div>; }
