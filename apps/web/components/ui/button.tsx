import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const buttonVariants = cva("inline-flex shrink-0 items-center justify-center gap-2 rounded-xl text-sm font-semibold transition-[transform,background-color,border-color,box-shadow,color] duration-200 active:translate-y-px disabled:pointer-events-none disabled:opacity-50 [&_svg]:size-4", { variants: { variant: { primary: "bg-primary text-white shadow-[0_7px_18px_rgba(36,74,165,.22)] hover:-translate-y-0.5 hover:bg-[#1c3d8d] hover:shadow-[0_10px_24px_rgba(36,74,165,.28)]", secondary: "border border-border bg-white text-foreground shadow-[0_2px_8px_rgba(16,37,80,.05)] hover:-translate-y-0.5 hover:border-blue-200 hover:bg-blue-50/50 hover:text-primary", ghost: "text-muted hover:bg-blue-50 hover:text-primary", danger: "bg-brand-red text-white shadow-[0_7px_18px_rgba(239,30,47,.2)] hover:-translate-y-0.5 hover:bg-[#cf1726]" }, size: { default: "h-10 px-4", sm: "h-8 rounded-lg px-3 text-xs", icon: "size-9 rounded-lg" } }, defaultVariants: { variant: "primary", size: "default" } });
export function Button({ className, variant, size, asChild = false, ...props }: React.ComponentProps<"button"> & VariantProps<typeof buttonVariants> & { asChild?: boolean }) { const Comp = asChild ? Slot : "button"; return <Comp className={cn(buttonVariants({ variant, size, className }))} {...props}/>; }
export { buttonVariants };
